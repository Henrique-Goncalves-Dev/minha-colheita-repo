{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/home/brcasp/Faculdade/MINHA COLHEITA/minha-colheita-repo\" && if git status --porcelain | grep -q .; then git add -A && git commit -m \"auto-sync: update back branch\" && git push origin back; fi 2>/dev/null || true",
            "statusMessage": "Syncing changes to back branch..."
          }
        ]
      }
    ]
  }
}
